spring:
  datasource:
    url: jdbc:postgresql://mrp-db:5432/mrp_bosch
    username: postgres
    password: root

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update